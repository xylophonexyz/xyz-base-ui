import {ComponentInterface, ComponentMetadata, ImageComponentMedia} from '../index';
import {Component} from './component';

export class ImageComponent extends Component implements ComponentInterface<ImageComponentMedia, ComponentMetadata> {
}
